#include <iostream>
#include <stdlib.h>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int k, n;
    cin>>k;
    cin>>n;
    int arr[n], i, H[100]={0};
    for(i=0; i<n; i++)
    {
        cin>>arr[i];
        H[arr[i]]++;
    }
        cout<<H[k]<<endl;
       return 0;
}
